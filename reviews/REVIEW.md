# Final evidence review

The independent INT-02 terminal could not write shared Git metadata under its default sandbox and remained input-blocked after that request was cancelled. Its terminal was closed explicitly. The coordinator performed the already-authorized Git integration, deterministic validation, critical visual review, and final indexing. This is not claimed as an independent-review pass.

## Accepted evidence

- User original: 1672×941 PNG, source/copy SHA256 match; small figure on a ridge under the Milky Way. Retained locally, unchanged.
- Obsidian: official Help repository image, 1989×1331; visually reviewed pale thin links, subdued surrounding nodes and purple local highlight. Indirect reference only; no application session captured.
- Cosmograph: desktop default, later clean loaded graph, and visibly magnified graph are real frames. The initial Playwright screenshot and its DOM/CSS were captured while loading, so they do not document the successful graph state. A click was issued but successful selection was not proven.
- Cosmograph original recording `page@c3193c8a6525a53b285a148e65a0b7f6.webm`: 93.80 seconds, 1280×720, 25 fps. Review at 76 seconds shows loading; 77–79 seconds shows the loaded graph; 80 seconds and later show the enlarged cluster. Recommended 15-second excerpt is source [77,92), with explicit derivation metadata.
- 100,000 Stars: live star field and onboarding captions, later exploration controls. Reviewed original timestamps 33/38/43/48 seconds and six frames from the worker's short excerpt. Wheel input is documented, but onboarding/automatic movement prevents a firm causal zoom claim. Recommended 18-second excerpt is source [33,51), showing entry into the exploration UI.
- Star Atlas: entry and attempted operation frames remain loading. Kept as blocked evidence, excluded from recommendations.

All new critical visual types and failed states were reviewed; repeated video frames were sampled above 20%. Frame sheets and contact sheet remain internal/local-only. Raw recordings were preserved, excerpts only trim/re-encode existing frames.

## Integrity and permission scope

`catalog/source-integrity.json` compares all eight captured upstream source/config/documentation files with retained originals, allowing Git newline conversion: 8/8 match. Fixed commit: `ce35edacf94dba1f02ef2b47957e937e4c9acf17`; package declares `@cosmos.gl/graph` 3.4.1. The included LICENCE is MIT. Original file text can be reused with the MIT notice; linked media and package dependencies are not automatically cleared. This is a partial source selection, not a runnable distribution. No source installation, build, or execution was performed.

Collection helpers were changed only to accept explicit existing-tool/output environment paths instead of machine user-directory defaults; syntax was checked without re-running capture. Worker handoffs are historical observations; the later `catalog/assets.json` and this review correct their premature or overbroad labels.

No authenticated/private browser session, full HAR, font binary or product API was collected. Browser profile/cache trees are excluded from asset indexes. Resource URL query strings were stripped by the original helpers, so the resource directory may omit content-affecting parameters; it is not a complete reproducible network archive. No narrow-screen observation or ground-to-sky footage was acquired.
