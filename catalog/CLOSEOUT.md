# 预算中止交付，核心采集未完成

已执行 3 个 Orca Worker（Run `run_d2f85b9f41de`），不是仅规划。主控 Astra high，CAP-01 Terra low，SRC-01 / REF-01 Luna low，实际会话设置已核对。主控未成功降档；重复系统/技能上下文使累计请求输入提前超出 200000 上限。触线后停止新增派发，INT-01 与镜头轨未启动。主控仅做保全、合并与必要许可修正；没有产品代码、安装或部署。

|来源|已保留|未取得|
|---|---|---|
|用户首图|原始 PNG，1672×941，视觉复核与源/副本 SHA256 相同|第三方底层权利核清、公开再分发许可|
|Obsidian|官方页面响应及清洗文本，间接文档证据|官方图谱截图、应用 DOM、选择/缩放操作|
|Cosmograph|隔离 profile/page 启动与失败记录|有效图谱截图、DOM/CSS、交互、真实录屏|
|cosmos.gl|固定提交的 8 个源码/配置/文档文件与补齐的 MIT LICENCE|完整依赖、运行验证、可运行示例所需其余文件|
|Star Atlas / 100,000 Stars|未访问，链接仅来自用户入口|全部镜头证据|
|Stellarium|未启动补位|天空/地平线实际操作|

原件目录 `.local-captures/` 仅本机有效，不在公开仓库；12 份 Worker 原件全部复制到主库并逐文件核对哈希，worktree 副本均保留。`file-inventory.json` 记录文件/尺寸/哈希/重复。`assets.json` 是主控纠正后的总索引；Worker 片段是当时交接记录，以总索引中的补充说明为准。

许可事故：SRC-01 首次推送时未获取正文，违背先许可再公开规则。收尾已按同一固定提交确认 MIT 并用修正提交 `84f801f` 补齐 LICENCE；未重写历史。这里只确认所收官方源码的仓库 MIT 许可，不将依赖或链接资源标为已清。文件保持静态，不是完整项目。

Orca 限制：REF-01 启动返回 agent_prompt_stalled，实际随后执行；其 worker_done 被已撤销 Dispatch 拒绝。成果按 Git/文件证据验收，生命周期仍失败。三个终端均请求释放；REF-01 返回 release_unknown，终端已关闭但进程停止未获确认，不能宣称全绿。

检查：`python catalog/verify.py` 检查文件可读、标准 JSON、PNG 解码和尺寸。原始 tsconfig.json 是带尾逗号的 JSONC，保留原样，未称严格 JSON 验证通过。首图视觉通过；其他无截图可复核。源码没有安装/执行测试。未制作联系表（仅一张有效图）。未部署、未开发前端。后续需要新的预算与明确继续指令补齐交互/镜头采集。
