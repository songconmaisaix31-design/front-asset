# MOT-01 — 100,000 Stars motion evidence

Collected from the one official interactive page in a new Playwright 1.62.0-alpha-1783623505000 context (`MOT-01-100000-stars-ctx-01`) using installed Chrome and a 1440x900 viewport. The first short observation remained at `Fetching stellar data`; the single justified alternate waited for `#loadtext` to become hidden (up to 120 seconds), after which the reviewed entry, zoom-in, and zoom-out frames show the live WebGL star field.

The local-only original recording is 51.24 seconds, VP8/WebM, 1440x900 at 25 fps; `100000-stars-motion-actions-15s.webm` is an explicitly disclosed 15.00-second temporal excerpt from its ready/action portion, not generated animation. Aligned DOM, computed CSS (`body`), raw and cleaned HTML, sanitized resource URLs, result metadata, all raw frames, and the full recording are in `.local-captures/MOT-01/100000-stars/`; no raw visual material is committed.

The captures are suitable only as a reference for a space/star-field entry and zoom/exploration affordance. They contain no ground-to-sky or terrestrial camera evidence, and source redistribution rights were not verified.

Actual model/effort: gpt-5.6-terra / low. Video metadata and every listed SHA-256 were rechecked after capture; critical keyframes and a frame from the 15-second excerpt were visually reviewed.
