# R2-MOT — Star Atlas Experience

The official `https://experience.staratlas.com/` was captured in isolated context `R2-MOT-star-atlas` at 1440×900 DPR 1. In the 2026-09-10 attempt, the first state was the familiar loading screen; after the finite wait, it exposed the public marketing shell (`Experience`, `Play now`) but the central loader persisted. A direct exact-text `PLAY NOW` click did not become actionable during its five-second locator timeout; no credentials or account state were supplied.

The unmodified 36.92-second recording, screenshots, aligned DOM and computed key-container CSS are local-only under the two manifest paths. Its observed transition is real but does **not** meet P0-D: no exploration entry, enter interaction, end state, camera behavior, WebGL canvas, or resource/console failure was established; the listener recorded no page errors and no HTTP response >=400. The public capture script is collection-only and has passed `node --check`; it preserves an earlier recording failure/attempt in the ignored raw directory.

Acquisition: public official browser observation. Evidence type: same-session screenshots/DOM/CSS and unmodified browser recording. Usage: blocked/reference diagnostic only; official public observation is not a redistribution grant.
